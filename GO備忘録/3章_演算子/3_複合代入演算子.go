package main

func main() {
	a := 10
	a += 1  // a = a + 1と同義
	a -= 1  // a = a - 1と同義
	a *= 1  // a = a * 1と同義
	a /= 1  // a = a / 1と同義
	a %= 1  // a = a % 1と同義
	a &= 1  // a = a & 1と同義
	a |= 1  // a = a | 1と同義
	a ^= 1  // a = a ^ 1と同義
	a <<= 1 // a = a << 1と同義
	a >>= 1 // a = a >> 1と同義
	a %= 1  // a = a % 1と同義
	a &= 1  // a = a & 1と同義
	a |= 1  // a = a | 1と同義
	a ^= 1  // a = a ^ 1と同義
	a <<= 1 // a = a << 1と同義
	a >>= 1 // a = a >> 1と同義
}
